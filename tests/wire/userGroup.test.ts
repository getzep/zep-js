// This file was auto-generated by Fern from our API Definition.

import * as Zep from "../../src/api/index";
import { ZepClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("UserGroupClient", () => {
    test("get (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {
            created_at: "created_at",
            description: "description",
            name: "name",
            updated_at: "updated_at",
            uuid: "uuid",
            version: 1,
        };

        server
            .mockEndpoint()
            .get("/user-groups/group_uuid")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.userGroup.get("group_uuid");
        expect(response).toEqual({
            createdAt: "created_at",
            description: "description",
            name: "name",
            updatedAt: "updated_at",
            uuid: "uuid",
            version: 1,
        });
    });

    test("get (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/user-groups/group_uuid")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.userGroup.get("group_uuid");
        }).rejects.toThrow(Zep.BadRequestError);
    });

    test("get (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/user-groups/group_uuid")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.userGroup.get("group_uuid");
        }).rejects.toThrow(Zep.UnauthorizedError);
    });

    test("get (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/user-groups/group_uuid")
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.userGroup.get("group_uuid");
        }).rejects.toThrow(Zep.ForbiddenError);
    });

    test("get (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/user-groups/group_uuid")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.userGroup.get("group_uuid");
        }).rejects.toThrow(Zep.NotFoundError);
    });

    test("list_for_user (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {
            items: [
                {
                    created_at: "created_at",
                    description: "description",
                    name: "name",
                    updated_at: "updated_at",
                    uuid: "uuid",
                    version: 1,
                },
            ],
            next_cursor: "next_cursor",
            total_size: 1,
        };

        server
            .mockEndpoint({ once: false })
            .get("/users/user_uuid/user-groups")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const expected = {
            items: [
                {
                    createdAt: "created_at",
                    description: "description",
                    name: "name",
                    updatedAt: "updated_at",
                    uuid: "uuid",
                    version: 1,
                },
            ],
            nextCursor: "next_cursor",
            totalSize: 1,
        };
        const page = await client.userGroup.listForUser("user_uuid", {
            limit: 1,
            cursor: "cursor",
        });

        expect(expected.items).toEqual(page.data);
        expect(page.hasNextPage()).toBe(true);
        const nextPage = await page.getNextPage();
        expect(expected.items).toEqual(nextPage.data);
    });

    test("list_for_user (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/users/user_uuid/user-groups")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.userGroup.listForUser("user_uuid");
        }).rejects.toThrow(Zep.BadRequestError);
    });

    test("list_for_user (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/users/user_uuid/user-groups")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.userGroup.listForUser("user_uuid");
        }).rejects.toThrow(Zep.UnauthorizedError);
    });

    test("list_for_user (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/users/user_uuid/user-groups")
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.userGroup.listForUser("user_uuid");
        }).rejects.toThrow(Zep.ForbiddenError);
    });

    test("list_for_user (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/users/user_uuid/user-groups")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.userGroup.listForUser("user_uuid");
        }).rejects.toThrow(Zep.NotFoundError);
    });

    test("list_for_user (6)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/users/user_uuid/user-groups")
            .respondWith()
            .statusCode(409)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.userGroup.listForUser("user_uuid");
        }).rejects.toThrow(Zep.ConflictError);
    });
});
