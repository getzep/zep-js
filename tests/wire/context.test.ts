// This file was auto-generated by Fern from our API Definition.

import * as Zep from "../../src/api/index";
import { ZepClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("ContextClient", () => {
    test("get_template (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { created_at: "created_at", name: "name", template: "template", uuid: "uuid" };

        server
            .mockEndpoint()
            .get("/context-templates/template_uuid")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.context.getTemplate("template_uuid");
        expect(response).toEqual({
            createdAt: "created_at",
            name: "name",
            template: "template",
            uuid: "uuid",
        });
    });

    test("get_template (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/context-templates/template_uuid")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.context.getTemplate("template_uuid");
        }).rejects.toThrow(Zep.BadRequestError);
    });

    test("get_template (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/context-templates/template_uuid")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.context.getTemplate("template_uuid");
        }).rejects.toThrow(Zep.UnauthorizedError);
    });

    test("get_template (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/context-templates/template_uuid")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.context.getTemplate("template_uuid");
        }).rejects.toThrow(Zep.NotFoundError);
    });
});
