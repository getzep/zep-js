/**
 * This file was auto-generated by Fern from our API Definition.
 */

import { mockServerPool } from "../mock-server/MockServerPool";
import { ZepClient } from "../../src/Client";

describe("Context", () => {
    test("list_context_templates", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { templates: [{ template: "template", template_id: "template_id", uuid: "uuid" }] };
        server.mockEndpoint().get("/context-templates").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.context.listContextTemplates();
        expect(response).toEqual({
            templates: [
                {
                    template: "template",
                    templateId: "template_id",
                    uuid: "uuid",
                },
            ],
        });
    });

    test("create_context_template", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = { template: "template", template_id: "template_id" };
        const rawResponseBody = { template: "template", template_id: "template_id", uuid: "uuid" };
        server
            .mockEndpoint()
            .post("/context-templates")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.context.createContextTemplate({
            template: "template",
            templateId: "template_id",
        });
        expect(response).toEqual({
            template: "template",
            templateId: "template_id",
            uuid: "uuid",
        });
    });

    test("get_context_template", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { template: "template", template_id: "template_id", uuid: "uuid" };
        server
            .mockEndpoint()
            .get("/context-templates/template_id")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.context.getContextTemplate("template_id");
        expect(response).toEqual({
            template: "template",
            templateId: "template_id",
            uuid: "uuid",
        });
    });

    test("update_context_template", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = { template: "template" };
        const rawResponseBody = { template: "template", template_id: "template_id", uuid: "uuid" };
        server
            .mockEndpoint()
            .put("/context-templates/template_id")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.context.updateContextTemplate("template_id", {
            template: "template",
        });
        expect(response).toEqual({
            template: "template",
            templateId: "template_id",
            uuid: "uuid",
        });
    });

    test("delete_context_template", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { message: "message" };
        server
            .mockEndpoint()
            .delete("/context-templates/template_id")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.context.deleteContextTemplate("template_id");
        expect(response).toEqual({
            message: "message",
        });
    });
});
