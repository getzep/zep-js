// This file was auto-generated by Fern from our API Definition.

import * as Zep from "../../src/api/index";
import { ZepClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("TaskClient", () => {
    test("list (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {
            items: [
                {
                    completed_at: "completed_at",
                    created_at: "created_at",
                    result: { key: "value" },
                    started_at: "started_at",
                    status: "status",
                    type: "type",
                    updated_at: "updated_at",
                    uuid: "uuid",
                },
            ],
            next_cursor: "next_cursor",
            total_size: 1,
        };

        server
            .mockEndpoint({ once: false })
            .get("/tasks")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const expected = {
            items: [
                {
                    completedAt: "completed_at",
                    createdAt: "created_at",
                    result: {
                        key: "value",
                    },
                    startedAt: "started_at",
                    status: "status",
                    type: "type",
                    updatedAt: "updated_at",
                    uuid: "uuid",
                },
            ],
            nextCursor: "next_cursor",
            totalSize: 1,
        };
        const page = await client.task.list({
            limit: 1,
            cursor: "cursor",
        });

        expect(expected.items).toEqual(page.data);
        expect(page.hasNextPage()).toBe(true);
        const nextPage = await page.getNextPage();
        expect(expected.items).toEqual(nextPage.data);
    });

    test("list (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server.mockEndpoint().get("/tasks").respondWith().statusCode(400).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.task.list();
        }).rejects.toThrow(Zep.BadRequestError);
    });

    test("list (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server.mockEndpoint().get("/tasks").respondWith().statusCode(401).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.task.list();
        }).rejects.toThrow(Zep.UnauthorizedError);
    });

    test("list (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server.mockEndpoint().get("/tasks").respondWith().statusCode(404).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.task.list();
        }).rejects.toThrow(Zep.NotFoundError);
    });

    test("get (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {
            completed_at: "completed_at",
            created_at: "created_at",
            error: {
                code: "code",
                details: { key: "value" },
                message: "message",
                param: "param",
                request_id: "request_id",
            },
            progress: { stage: "stage" },
            result: { key: "value" },
            started_at: "started_at",
            status: "status",
            type: "type",
            updated_at: "updated_at",
            uuid: "uuid",
        };

        server.mockEndpoint().get("/tasks/task_uuid").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.task.get("task_uuid");
        expect(response).toEqual({
            completedAt: "completed_at",
            createdAt: "created_at",
            error: {
                code: "code",
                details: {
                    key: "value",
                },
                message: "message",
                param: "param",
                requestId: "request_id",
            },
            progress: {
                stage: "stage",
            },
            result: {
                key: "value",
            },
            startedAt: "started_at",
            status: "status",
            type: "type",
            updatedAt: "updated_at",
            uuid: "uuid",
        });
    });

    test("get (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server.mockEndpoint().get("/tasks/task_uuid").respondWith().statusCode(400).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.task.get("task_uuid");
        }).rejects.toThrow(Zep.BadRequestError);
    });

    test("get (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server.mockEndpoint().get("/tasks/task_uuid").respondWith().statusCode(401).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.task.get("task_uuid");
        }).rejects.toThrow(Zep.UnauthorizedError);
    });

    test("get (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server.mockEndpoint().get("/tasks/task_uuid").respondWith().statusCode(404).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.task.get("task_uuid");
        }).rejects.toThrow(Zep.NotFoundError);
    });
});
