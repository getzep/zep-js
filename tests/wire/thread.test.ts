// This file was auto-generated by Fern from our API Definition.

import * as Zep from "../../src/api/index";
import { ZepClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("ThreadClient", () => {
    test("list (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {
            items: [
                {
                    created_at: "created_at",
                    graph_uuid: "graph_uuid",
                    thread_id: "thread_id",
                    updated_at: "updated_at",
                    user_uuid: "user_uuid",
                    uuid: "uuid",
                },
            ],
            next_cursor: "next_cursor",
            total_size: 1,
        };

        server
            .mockEndpoint({ once: false })
            .get("/threads")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const expected = {
            items: [
                {
                    createdAt: "created_at",
                    graphUuid: "graph_uuid",
                    threadId: "thread_id",
                    updatedAt: "updated_at",
                    userUuid: "user_uuid",
                    uuid: "uuid",
                },
            ],
            nextCursor: "next_cursor",
            totalSize: 1,
        };
        const page = await client.thread.list({
            limit: 1,
            cursor: "cursor",
            orderBy: "order_by",
            order: "order",
            userUuid: "user_uuid",
        });

        expect(expected.items).toEqual(page.data);
        expect(page.hasNextPage()).toBe(true);
        const nextPage = await page.getNextPage();
        expect(expected.items).toEqual(nextPage.data);
    });

    test("list (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server.mockEndpoint().get("/threads").respondWith().statusCode(400).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.thread.list();
        }).rejects.toThrow(Zep.BadRequestError);
    });

    test("list (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server.mockEndpoint().get("/threads").respondWith().statusCode(401).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.thread.list();
        }).rejects.toThrow(Zep.UnauthorizedError);
    });

    test("list (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server.mockEndpoint().get("/threads").respondWith().statusCode(404).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.thread.list();
        }).rejects.toThrow(Zep.NotFoundError);
    });

    test("get (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {
            created_at: "created_at",
            graph_uuid: "graph_uuid",
            thread_id: "thread_id",
            updated_at: "updated_at",
            user_uuid: "user_uuid",
            uuid: "uuid",
        };

        server
            .mockEndpoint()
            .get("/threads/thread_uuid")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.thread.get("thread_uuid");
        expect(response).toEqual({
            createdAt: "created_at",
            graphUuid: "graph_uuid",
            threadId: "thread_id",
            updatedAt: "updated_at",
            userUuid: "user_uuid",
            uuid: "uuid",
        });
    });

    test("get (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/threads/thread_uuid")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.thread.get("thread_uuid");
        }).rejects.toThrow(Zep.BadRequestError);
    });

    test("get (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/threads/thread_uuid")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.thread.get("thread_uuid");
        }).rejects.toThrow(Zep.UnauthorizedError);
    });

    test("get (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/threads/thread_uuid")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.thread.get("thread_uuid");
        }).rejects.toThrow(Zep.NotFoundError);
    });

    test("get_context (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { context: "context" };

        server
            .mockEndpoint()
            .get("/threads/thread_uuid/context")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.thread.getContext("thread_uuid", {
            templateUuid: "template_uuid",
        });
        expect(response).toEqual({
            context: "context",
        });
    });

    test("get_context (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/threads/thread_uuid/context")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.thread.getContext("thread_uuid");
        }).rejects.toThrow(Zep.BadRequestError);
    });

    test("get_context (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/threads/thread_uuid/context")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.thread.getContext("thread_uuid");
        }).rejects.toThrow(Zep.UnauthorizedError);
    });

    test("get_context (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/threads/thread_uuid/context")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.thread.getContext("thread_uuid");
        }).rejects.toThrow(Zep.NotFoundError);
    });

    test("list_episodes (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { items: [{ key: "value" }], next_cursor: "next_cursor", total_size: 1 };

        server
            .mockEndpoint({ once: false })
            .get("/threads/thread_uuid/episodes")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const expected = {
            items: [
                {
                    key: "value",
                },
            ],
            nextCursor: "next_cursor",
            totalSize: 1,
        };
        const page = await client.thread.listEpisodes("thread_uuid", {
            limit: 1,
            cursor: "cursor",
        });

        expect(expected.items).toEqual(page.data);
        expect(page.hasNextPage()).toBe(true);
        const nextPage = await page.getNextPage();
        expect(expected.items).toEqual(nextPage.data);
    });

    test("list_episodes (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/threads/thread_uuid/episodes")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.thread.listEpisodes("thread_uuid");
        }).rejects.toThrow(Zep.BadRequestError);
    });

    test("list_episodes (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/threads/thread_uuid/episodes")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.thread.listEpisodes("thread_uuid");
        }).rejects.toThrow(Zep.UnauthorizedError);
    });

    test("list_episodes (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/threads/thread_uuid/episodes")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.thread.listEpisodes("thread_uuid");
        }).rejects.toThrow(Zep.NotFoundError);
    });

    test("list_messages (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {
            items: [
                {
                    content: "content",
                    created_at: "created_at",
                    metadata: { key: "value" },
                    name: "name",
                    processed: true,
                    role: "role",
                    thread_uuid: "thread_uuid",
                    uuid: "uuid",
                },
            ],
            next_cursor: "next_cursor",
            total_size: 1,
        };

        server
            .mockEndpoint({ once: false })
            .get("/threads/thread_uuid/messages")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const expected = {
            items: [
                {
                    content: "content",
                    createdAt: "created_at",
                    metadata: {
                        key: "value",
                    },
                    name: "name",
                    processed: true,
                    role: "role",
                    threadUuid: "thread_uuid",
                    uuid: "uuid",
                },
            ],
            nextCursor: "next_cursor",
            totalSize: 1,
        };
        const page = await client.thread.listMessages("thread_uuid", {
            limit: 1,
            cursor: "cursor",
        });

        expect(expected.items).toEqual(page.data);
        expect(page.hasNextPage()).toBe(true);
        const nextPage = await page.getNextPage();
        expect(expected.items).toEqual(nextPage.data);
    });

    test("list_messages (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/threads/thread_uuid/messages")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.thread.listMessages("thread_uuid");
        }).rejects.toThrow(Zep.BadRequestError);
    });

    test("list_messages (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/threads/thread_uuid/messages")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.thread.listMessages("thread_uuid");
        }).rejects.toThrow(Zep.UnauthorizedError);
    });

    test("list_messages (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/threads/thread_uuid/messages")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.thread.listMessages("thread_uuid");
        }).rejects.toThrow(Zep.NotFoundError);
    });

    test("get_summary (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {
            created_at: "created_at",
            last_summarized_at: "last_summarized_at",
            last_summarized_episode_valid_at: "last_summarized_episode_valid_at",
            summary: "summary",
            thread_uuid: "thread_uuid",
            uuid: "uuid",
        };

        server
            .mockEndpoint()
            .get("/threads/thread_uuid/summary")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.thread.getSummary("thread_uuid");
        expect(response).toEqual({
            createdAt: "created_at",
            lastSummarizedAt: "last_summarized_at",
            lastSummarizedEpisodeValidAt: "last_summarized_episode_valid_at",
            summary: "summary",
            threadUuid: "thread_uuid",
            uuid: "uuid",
        });
    });

    test("get_summary (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/threads/thread_uuid/summary")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.thread.getSummary("thread_uuid");
        }).rejects.toThrow(Zep.BadRequestError);
    });

    test("get_summary (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/threads/thread_uuid/summary")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.thread.getSummary("thread_uuid");
        }).rejects.toThrow(Zep.UnauthorizedError);
    });

    test("get_summary (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/threads/thread_uuid/summary")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.thread.getSummary("thread_uuid");
        }).rejects.toThrow(Zep.NotFoundError);
    });
});
