// This file was auto-generated by Fern from our API Definition.

import * as Zep from "../../src/api/index";
import { ZepClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("BatchClient", () => {
    test("list (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {
            items: [
                {
                    created_at: "created_at",
                    metadata: { key: "value" },
                    progress: { key: "value" },
                    status: "status",
                    strict_ontology: true,
                    uuid: "uuid",
                },
            ],
            next_cursor: "next_cursor",
            total_size: 1,
        };

        server
            .mockEndpoint({ once: false })
            .get("/batches")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const expected = {
            items: [
                {
                    createdAt: "created_at",
                    metadata: {
                        key: "value",
                    },
                    progress: {
                        key: "value",
                    },
                    status: "status",
                    strictOntology: true,
                    uuid: "uuid",
                },
            ],
            nextCursor: "next_cursor",
            totalSize: 1,
        };
        const page = await client.batch.list({
            limit: 1,
            cursor: "cursor",
            status: "status",
        });

        expect(expected.items).toEqual(page.data);
        expect(page.hasNextPage()).toBe(true);
        const nextPage = await page.getNextPage();
        expect(expected.items).toEqual(nextPage.data);
    });

    test("list (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server.mockEndpoint().get("/batches").respondWith().statusCode(400).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.batch.list();
        }).rejects.toThrow(Zep.BadRequestError);
    });

    test("list (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server.mockEndpoint().get("/batches").respondWith().statusCode(401).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.batch.list();
        }).rejects.toThrow(Zep.UnauthorizedError);
    });

    test("list (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server.mockEndpoint().get("/batches").respondWith().statusCode(404).jsonBody(rawResponseBody).build();

        await expect(async () => {
            return await client.batch.list();
        }).rejects.toThrow(Zep.NotFoundError);
    });

    test("get (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {
            created_at: "created_at",
            metadata: { key: "value" },
            progress: { key: "value" },
            status: "status",
            strict_ontology: true,
            uuid: "uuid",
        };

        server
            .mockEndpoint()
            .get("/batches/batch_uuid")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.batch.get("batch_uuid");
        expect(response).toEqual({
            createdAt: "created_at",
            metadata: {
                key: "value",
            },
            progress: {
                key: "value",
            },
            status: "status",
            strictOntology: true,
            uuid: "uuid",
        });
    });

    test("get (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/batches/batch_uuid")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.batch.get("batch_uuid");
        }).rejects.toThrow(Zep.BadRequestError);
    });

    test("get (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/batches/batch_uuid")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.batch.get("batch_uuid");
        }).rejects.toThrow(Zep.UnauthorizedError);
    });

    test("get (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/batches/batch_uuid")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.batch.get("batch_uuid");
        }).rejects.toThrow(Zep.NotFoundError);
    });

    test("list_items (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = { items: [{ key: "value" }], next_cursor: "next_cursor", total_size: 1 };

        server
            .mockEndpoint({ once: false })
            .get("/batches/batch_uuid/items")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const expected = {
            items: [
                {
                    key: "value",
                },
            ],
            nextCursor: "next_cursor",
            totalSize: 1,
        };
        const page = await client.batch.listItems("batch_uuid", {
            limit: 1,
            cursor: "cursor",
        });

        expect(expected.items).toEqual(page.data);
        expect(page.hasNextPage()).toBe(true);
        const nextPage = await page.getNextPage();
        expect(expected.items).toEqual(nextPage.data);
    });

    test("list_items (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/batches/batch_uuid/items")
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.batch.listItems("batch_uuid");
        }).rejects.toThrow(Zep.BadRequestError);
    });

    test("list_items (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/batches/batch_uuid/items")
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.batch.listItems("batch_uuid");
        }).rejects.toThrow(Zep.UnauthorizedError);
    });

    test("list_items (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {};

        server
            .mockEndpoint()
            .get("/batches/batch_uuid/items")
            .respondWith()
            .statusCode(404)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.batch.listItems("batch_uuid");
        }).rejects.toThrow(Zep.NotFoundError);
    });
});
