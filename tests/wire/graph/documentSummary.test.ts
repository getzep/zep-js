/**
 * This file was auto-generated by Fern from our API Definition.
 */

import { mockServerPool } from "../../mock-server/MockServerPool";
import { ZepClient } from "../../../src/Client";

describe("DocumentSummary", () => {
    test("get_by_graph_id", async () => {
        const server = mockServerPool.createServer();
        const client = new ZepClient({ apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = {};
        const rawResponseBody = [
            {
                created_at: "created_at",
                document_id: "document_id",
                last_summarized_at: "last_summarized_at",
                last_summarized_episode_valid_at: "last_summarized_episode_valid_at",
                summary: "summary",
                uuid: "uuid",
            },
        ];
        server
            .mockEndpoint()
            .post("/graph/document-summary/graph/graph_id")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.graph.documentSummary.getByGraphId("graph_id");
        expect(response).toEqual([
            {
                createdAt: "created_at",
                documentId: "document_id",
                lastSummarizedAt: "last_summarized_at",
                lastSummarizedEpisodeValidAt: "last_summarized_episode_valid_at",
                summary: "summary",
                uuid: "uuid",
            },
        ]);
    });
});
